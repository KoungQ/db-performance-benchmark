apiVersion: 1
providers:
  - name: "db-benchmark"
    orgId: 1
    folder: "Benchmark"
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /etc/grafana/provisioning/dashboards/json
